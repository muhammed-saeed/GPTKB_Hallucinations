
SYSTEM: Return JSON only. No prose, no markdown, no code fences.

Perform named entity recognition (NER) on the topic of "{{ root_subject }}".
Each line below is a phrase.
Classify whether it is a topic-relevant named entity (true) or not (false).

Input lines:
{{ phrases_block }}

Return:
{
  "phrases": [
    {"phrase":"<line>","is_ne": true|false}
  ]
}

Rules:
1. is_ne = true for specific named entities clearly related to "{{ root_subject }}" (people, characters, works, events, places, organizations, artifacts).
2. is_ne = false for literals, numbers, dates, URLs, or unrelated phrases to the topic "{{ root_subject }}".
3. Return only valid JSON as shown. No prose or commentary.
